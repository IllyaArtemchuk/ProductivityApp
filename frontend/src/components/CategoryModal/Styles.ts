import { makeStyles } from "@material-ui/core/styles";

export const CategoryModalStyles = makeStyles({
  ModalTitle: {
    textAlign: "center",
  },
  CategoryTextField: {
    width: 200,
    textAlign: "center",
  },
});
